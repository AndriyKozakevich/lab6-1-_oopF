#ifndef AVERAGE_H
#define AVERAGE_H

#include <cstddef> // Для size_t

// Шаблонна функція для обчислення середнього арифметичного масиву
template <typename T>
double calculateAverage(const T* arr, size_t size);

#endif // AVERAGE_H
